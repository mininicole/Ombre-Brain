# Ombre Brain on Fly.io
# 单一记忆源，给 Claude Desktop / Evan / Kelivo 等所有客户端用

app = 'ombre-brain'
primary_region = 'nrt'

[build]

[env]
  OMBRE_TRANSPORT = 'streamable-http'
  OMBRE_BUCKETS_DIR = '/app/buckets'

[http_service]
  internal_port = 8000
  force_https = true
  # 挂起模式：闲时省钱，请求进来时 Fly 自动唤醒（首次响应慢 1-2s）
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = '60s'
    timeout = '5s'
    grace_period = '15s'
    method = 'GET'
    path = '/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'

[mounts]
  source = 'ombre_data'
  destination = '/app/buckets'
